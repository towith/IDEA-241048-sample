apply plugin: "war"

bootWar {
    mainClassName = "com.willbe.wordl.WordlearnbackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
